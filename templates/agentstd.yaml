version: 1

# projectOnly: false  # set true to skip ~/.agentstd.yaml + ~/.agents/skills/ (CLI: --project-only)

targets:
  - claude

hooks:
  preToolUse:
    command: node .agentstd/hooks/pretooluse.js

skills:
  dir: .agents/skills
  homeDir: .agents/skills

instructions:
  shared: .agentstd/instructions/shared.md
